import 'package:flutter/material.dart';

class DetailBook extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Book Detail of XYZ')
      ),
      body: Center(
        child: Text('Yeaaaaaaah'),
      ),
    );
  }

}